{
  "name": "opendonate",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "deploy": "cd frontend && yarn install && yarn build && cp -rf ./build/* ../src/public && cd .. && yarn start"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "express-subdomain": "^1.0.5",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "morgan": "~1.9.1",
    "node-git": "^0.0.0"
  }
}
